schema {
  query: Query
}

type User {
  id: Int
  username: String
}

type Query {
  login(username: String, password: String): User
}

type Mutation {
  signup(username: String!, password: String!): User
}
